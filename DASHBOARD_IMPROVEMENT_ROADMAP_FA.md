# نقشه راه ارتقای داشبورد اختصاصی سیتباک

این فایل از Stage 52 به پروژه اضافه شد تا ادامه توسعه داشبورد مرحله‌به‌مرحله و بدون rollback انجام شود.

## اصل‌های ادامه کار

- همیشه از آخرین ZIP معتبر ادامه داده شود.
- هر مرحله در `CHANGELOG.md` ثبت شود.
- تغییرات قبلی صفحه اصلی، امکانات، درباره ما، موبایل، ربات بله و SEO حفظ شود.
- اگر migration لازم شد، در پاسخ نهایی دستور `python manage.py migrate` اعلام شود.
- اولویت با قابلیت‌های واقعی و قابل استفاده در داشبورد است، نه فقط ظاهر.

## ترتیب پیشنهادی مراحل

### Stage 52 - نقشه راه اجرایی داشبورد
- افزودن صفحه «نقشه راه داشبورد» داخل پنل.
- ثبت این فایل برای ادامه کار در چت‌های بعدی.
- مشخص کردن مرحله بعدی: قیمت‌ها، پلن‌ها و جدول مقایسه.

### Stage 53 - ویرایش واقعی قیمت‌ها، پلن‌ها و جدول مقایسه ✅ انجام شد
- مدل‌های `PricingPlan` و `PricingComparisonRow` اضافه شدند.
- ویرایش نام پلن، قیمت، متن دکمه، رنگ، ترتیب، ویژگی‌ها و وضعیت فعال از داشبورد ممکن شد.
- جدول مقایسه صفحه قیمت‌ها، جدول پکیج‌های اشتراکی و جدول کامل صفحه پلن‌ها از داشبورد قابل مدیریت شدند.
- داده‌های اولیه با migration جدید seed می‌شوند و fallback قبلی برای نصب‌های بدون migration حفظ شده است.

### Stage 54 - اصلاح پاسخ تکراری ربات بله ✅ انجام شد
- قفل دیتابیسی برای جلوگیری از اجرای چند poller همزمان اضافه شد.
- update_id پیام‌های دریافتی بله یکتا شد.
- اجرای خودکار ربات همراه سایت حفظ شد و پاسخ تکراری کنترل شد.

### Stage 55 - مدیریت رسانه‌ها و ویدیوها ✅ انجام شد
- مدل `MediaAsset` برای مدیریت رسانه‌ها اضافه شد.
- گالری media با preview، فیلتر، جستجو، وضعیت فعال و اندازه فایل پیاده‌سازی شد.
- upload امن تصویر، ویدیو و سند از داشبورد اضافه شد.
- کلیدهای محل استفاده برای Hero، کاورها، تیم، بلاگ و OG image آماده شد.

### Stage 56 - نقش‌ها، دسترسی‌ها و امنیت ✅ انجام شد
- نقش‌های مدیر کل، محتوا، فروش، پشتیبانی و SEO.
- محدودسازی منو و عملیات بر اساس نقش.
- audit log برای تغییرات مهم.
- نمایش آخرین ورود، IP و وضعیت امنیت حساب‌ها.

### Stage 57 - CRM سبک برای لیدها ✅ انجام شد
- نمای Kanban لیدها و دموها.
- یادآوری پیگیری بعدی.
- ثبت برچسب، منبع، یادداشت و تاریخچه تماس.
- خروجی کاربردی فروش.

### Stage 58 - مرکز عملیات دمو ✅ انجام شد
- مدیریت کامل لینک امن دمو.
- ثبت مشاهده/ورود/ارسال لینک.
- قالب پیام آماده برای ارسال به مشتری.
- گزارش تبدیل درخواست دمو به مشتری.

### Stage 59 - کنترل‌سنتر ربات بله ✅ انجام شد
- ویرایش سناریوها و متن‌های ربات از داشبورد.
- سلامت اتصال، آخرین polling و خطاهای API.
- pause/resume ربات از پنل.
- قالب پاسخ اپراتور.

### Stage 60 - صفحه‌ساز سبک سایت ✅ انجام شد
- مدیریت سکشن‌های صفحات با عنوان‌های فارسی.
- ترتیب‌دهی ساده یا drag/drop.
- پیش‌نویس/انتشار.
- پیش‌نمایش قبل از انتشار.

### Stage 61 - Content Studio برای بلاگ و FAQ
- دسته‌بندی، برچسب، وضعیت ویژه.
- preview بهتر محتوا.
- چک‌لیست SEO مقاله و FAQ.
- مرتب‌سازی FAQها.

### Stage 62 - ابزارهای SEO و Redirect
- چک‌لیست SEO هر صفحه.
- مدیریت robots و llms.
- redirectهای 301/302.
- گزارش صفحات ناقص از نظر SEO.

### Stage 63 - گزارش‌های مدیریتی
- نمودار لیدها، پیام‌ها و دموها.
- نرخ تبدیل لید به دمو و مشتری.
- گزارش منابع ورودی و کمپین‌ها.
- خروجی مدیریتی ماهانه.

### Stage 64 - سلامت production، بکاپ و مانیتورینگ
- وضعیت migration، static و media.
- هشدار env ناقص.
- راهنمای بکاپ دیتابیس و media.
- گزارش خطاهای اخیر و آخرین deploy.

## مرحله بعدی دقیق

مرحله بعدی پیشنهادی: **Stage 61 - Content Studio برای بلاگ و FAQ**.


## Stage 54 - اصلاح پاسخ تکراری ربات بله
- ادامه از Stage 53 بدون rollback انجام شد.
- مشکل پاسخ چندباره ربات بله به یک پیام، که در حالت اجرای خودکار همراه سایت و چند worker/process ایجاد می‌شد، با قفل دیتابیسی poller اصلاح شد.
- برای هر پیام دریافتی بله، شناسه update در جدول پیام‌ها یکتا شد تا حتی اگر چند پردازش همزمان همان پیام را ببینند، فقط اولین پردازش پاسخ ارسال کند.
- migration جدید رکوردهای تکراری قبلی را پاکسازی و constraint یکتا را اضافه می‌کند.
- اجرای خودکار ربات همراه سایت حفظ شد، اما دیگر چند poller همزمان نباید باعث ارسال چند پاسخ پشت سر هم شوند.
- بعد از نصب این نسخه اجرای `python manage.py migrate` ضروری است.


## Stage 55 - مدیریت رسانه‌ها و ویدیوها از داشبورد
- ادامه از Stage 54 بدون rollback انجام شد.
- مدل `MediaAsset` و migration جدید برای ثبت تصویر، ویدیو، سند و سایر فایل‌های عمومی سایت اضافه شد.
- صفحه واقعی `/dashboard/media/` جایگزین placeholder قبلی شد و شامل آپلود فایل، preview، جستجو، فیلتر نوع رسانه، فعال/غیرفعال‌سازی و حذف رکورد است.
- صفحه ویرایش رسانه برای تغییر عنوان، نوع، فایل، Alt، محل استفاده پیشنهادی و توضیح داخلی اضافه شد.
- راهنمای کلیدهای استفاده برای ویدیوی Hero، کاورهای صفحه اصلی، اعضای درباره ما و OG image داخل داشبورد اضافه شد.
- مرحله بعدی roadmap به Stage 56 برای نقش‌ها، دسترسی‌ها و امنیت داشبورد منتقل شد.


## Stage 56 - انجام شد
- نقش‌های داشبورد، کنترل منوها، کنترل URLهای مستقیم و گزارش تغییرات مهم اضافه شد.
- صفحه امنیت پنل شامل کاربران مجاز، نقش‌ها، چک‌لیست امنیتی و Audit Log ساخته شد.
- مسیر ادامه roadmap: Stage 57 برای CRM سبک و Kanban لیدها.

## وضعیت Stage 58
- Stage 58 با تمرکز بر مدیریت دمو و لینک امن تکمیل شد.
- درخواست‌های دمو اکنون دارای مرکز عملیات، پیام آماده ارسال، تاریخچه رویدادهای لینک و عملیات تمدید/لغو/بازسازی هستند.
- مرحله بعدی: Stage 59 - ارتقای ربات بله و سناریوهای گفتگو.

## وضعیت Stage 59
- Stage 59 با تمرکز بر کنترل‌سنتر ربات بله تکمیل شد.
- سناریوهای گفتگو، کلمات محرک و عملیات هر سناریو از داشبورد قابل مدیریت شدند.
- قالب‌های پاسخ آماده اپراتور اضافه شد و در جزئیات گفتگو قابل ارسال است.
- مسیر ادامه roadmap: Stage 60 برای صفحه‌ساز سبک سکشن‌های سایت.


## وضعیت Stage 60
- Stage 60 با تمرکز بر صفحه‌ساز سبک تکمیل شد.
- سکشن‌های صفحات عمومی اکنون در داشبورد با عنوان فارسی، راهنما، نوع چیدمان، ترتیب و وضعیت انتشار مدیریت می‌شوند.
- صفحه جدید «صفحه‌ساز» به پنل اضافه شد و به نقش‌های محتوا، SEO و مدیر کل متصل شد.
- سکشن‌های منتشرنشده در سایت عمومی نمایش داده نمی‌شوند، اما اگر migration اجرا نشده باشد سایت با fallback قبلی از کار نمی‌افتد.
- مسیر ادامه roadmap: Stage 61 برای Content Studio بلاگ و FAQ.

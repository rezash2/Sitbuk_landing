# راهنمای Stage 32.7 - ربات بله سایت سیتباک

این مرحله زیرساخت ربات بله را به پروژه لندینگ سیتباک اضافه می‌کند. ربات برای ثبت درخواست مشاوره، درخواست مشاهده دمو و بررسی وضعیت درخواست آماده شده و داده‌ها را به مدل‌های داخلی سایت وصل می‌کند.

## متغیرهای محیطی لازم

در فایل `.env` یا تنظیمات سرور مقدارهای زیر را قرار بده:

```env
BALE_BOT_TOKEN=توکن_ربات_بله
BALE_BOT_USERNAME=نام_کاربری_ربات_بدون_@
BALE_WEBHOOK_SECRET=یک_عبارت_امن_اختیاری
BALE_BOT_API_BASE=https://tapi.bale.ai
```

اگر `BALE_WEBHOOK_SECRET` ست شود، آدرس webhook باید با همان secret فراخوانی شود.

## اجرای ربات با Polling

برای تست روی سرور یا سیستم محلی:

```bash
python manage.py poll_bale --once
python manage.py poll_bale
```

گزینه‌ها:

```bash
python manage.py poll_bale --limit 50 --timeout 25 --sleep 1
```

## اجرای ربات با Webhook

مسیرهای آماده:

```text
/bale/webhook/
/bale/webhook/<BALE_WEBHOOK_SECRET>/
```

در حالت production پیشنهاد می‌شود از مسیر دارای secret استفاده شود.

## سناریوی مکالمه

ربات منوی اصلی دارد:

- درخواست مشاوره
- مشاهده دمو
- وضعیت درخواست
- راه‌های تماس

### درخواست مشاوره

ربات دریافت می‌کند:

- نام و نام خانوادگی
- موبایل
- نام شرکت
- ایمیل اختیاری
- توضیح اختیاری

سپس یک `LeadRequest` با `source_page=bale_bot` ایجاد می‌شود.

### درخواست دمو

ربات دریافت می‌کند:

- نام و نام خانوادگی
- موبایل
- ایمیل
- نام شرکت
- نوع دمو: سیتباک، بهنیکو یا هر دو
- توضیح اختیاری

سپس یک `DemoRequest` ایجاد می‌شود و لینک امن `/demo/<token>/` برای کاربر ارسال می‌شود.

### وضعیت درخواست

کاربر با وارد کردن موبایل، آخرین وضعیت درخواست دمو یا مشاوره خود را می‌بیند.

## مدل‌های جدید

- `BaleBotSettings`: تنظیمات ربات، وضعیت فعال بودن و offset polling
- `BaleBotConversation`: گفتگو و state مکالمه هر chat
- `BaleBotMessage`: پیام‌های ورودی، خروجی و سیستمی

## نکته امنیتی

توکن ربات نباید داخل کد ذخیره شود. توکن فقط از متغیر محیطی خوانده می‌شود.

## مرحله بعد

در Stage 32.8 باید صفحه کامل مدیریت گفتگوهای بله داخل داشبورد اختصاصی ساخته شود:

- لیست گفتگوها
- صفحه جزئیات گفتگو
- ارسال پاسخ اپراتور از داشبورد به بله
- اتصال گفتگو به لید/دمو
- فعال/غیرفعال کردن ربات از پنل اختصاصی

## Stage 32.8 - مدیریت ربات بله در داشبورد اختصاصی
- صفحه `/dashboard/bale/` برای مدیریت گفتگوهای ربات بله ساخته شد.
- تنظیمات ربات، فیلتر گفتگوها، خروجی، صفحه جزئیات گفتگو، ریست جریان مکالمه و ارسال پاسخ اپراتور از داخل داشبورد اضافه شد.
- پیام‌های مرتبط با LeadRequest و DemoRequest در صفحه جزئیات قابل مشاهده و پیگیری هستند.


## Stage 59 - سناریوهای قابل مدیریت از داشبورد
- مسیر مدیریت سناریوها: `/dashboard/bale/scenarios/`
- هر سناریو شامل کلید، عنوان، کلمات محرک، نوع تطبیق، عملیات و متن پاسخ است.
- عملیات‌های پشتیبانی‌شده: پاسخ آماده، شروع مشاوره، شروع دمو، پیگیری وضعیت، راه‌های تماس و بازگشت به منوی اصلی.
- قالب‌های پاسخ اپراتور در همان صفحه مدیریت می‌شوند و در صفحه جزئیات گفتگو برای ارسال سریع نمایش داده می‌شوند.
- سناریوهای دیتابیس قبل از منطق پیش‌فرض بررسی می‌شوند؛ بنابراین می‌توان بدون تغییر کد، رفتار ربات را تنظیم کرد.

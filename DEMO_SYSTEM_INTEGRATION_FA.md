# راهنمای اتصال نسخه‌های دمو - Stage 32.5

در این مرحله سایت لندینگ بعد از ثبت فرم «مشاهده دمو» یک لینک امن و موقت می‌سازد و کاربر را به صفحه انتخاب دمو هدایت می‌کند.

## مسیرهای جدید

- صفحه امن انتخاب دمو: `/demo/<token>/`
- ورود به دمو: `/demo/<token>/<target>/`

مقادیر `target`:

- `sitbuk`
- `behnico`

## تنظیمات سرور

در فایل `.env` یا تنظیمات محیطی سرور مقدارهای زیر را تنظیم کن:

```env
BEHNICO_DEMO_BASE_URL=https://behnico-demo.sitbuk.com
SITBUK_DEMO_BASE_URL=https://erp-demo.sitbuk.com
DEMO_ACCESS_TOKEN_HOURS=72
```

وقتی کاربر روی دکمه ورود کلیک کند، سایت او را به آدرس دمو هدایت می‌کند و این پارامترها را اضافه می‌کند:

```text
?demo_token=<token>&source=sitbuk_landing&request_id=<id>
```

## نکته امنیتی

نسخه‌های دمو باید جدا از سامانه‌های اصلی اجرا شوند:

- دیتابیس جدا
- دامنه یا subdomain جدا
- اطلاعات نمونه و غیرواقعی
- کاربرهای demo محدود
- عدم اتصال به دیتابیس اصلی بهنیکو یا سیتباک

## رفتار فعلی

- بعد از ثبت فرم دمو، لینک امن ساخته می‌شود.
- لینک در جدول `DemoRequest` ذخیره می‌شود.
- لینک تا زمان تعیین‌شده در `DEMO_ACCESS_TOKEN_HOURS` معتبر است.
- ورود کاربر به دمو در `demo_entered_at`، `demo_launch_count` و `last_demo_target` ثبت می‌شود.

## Stage 32.6 - مدیریت درخواست‌های دمو در داشبورد اختصاصی
درخواست‌های ثبت‌شده از فرم دمو حالا در مسیر زیر قابل مدیریت هستند:

```text
/dashboard/demos/
```

در صفحه جزئیات هر درخواست می‌توانی لینک امن دمو را ببینی، آن را بازسازی کنی، زمان ارسال لینک را ثبت کنی، وضعیت را تغییر بدهی و یادداشت داخلی بنویسی.


## اتصال دمو به ربات بله - Stage 32.7
ربات بله بعد از ثبت درخواست مشاهده دمو، لینک امن `/demo/<token>/` را برای کاربر تولید و ارسال می‌کند. این لینک همان مکانیزم Stage 32.5 را استفاده می‌کند و برای دموهای سیتباک، بهنیکو یا هر دو قابل استفاده است.


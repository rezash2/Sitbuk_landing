# نقشه راه داشبورد ادمین اختصاصی سیتباک

از این مرحله به بعد، مسیر توسعه مدیریت محتوای سایت سیتباک بر پایه **پنل اختصاصی مدیریت** خواهد بود و وابستگی عملیاتی به پنل پیش‌فرض Django Admin حذف می‌شود. Django Admin می‌تواند فقط برای اضطرار یا سوپرادمین فنی باقی بماند، اما ابزار اصلی مدیریت سایت باید داشبورد جدید باشد.

## اصول قطعی از این مرحله به بعد

- هیچ rollback از مراحل قبلی انجام نشود.
- طراحی تیره/طلایی، RTL، فونت local، لوگو، صفحه اول compact، تصاویر Stage 20/21، WhiteNoise، static/media، SEO، بلاگ، FAQ و فرم‌ها حفظ شوند.
- CMS ساخته‌شده در Stage 23 تا Stage 26 حذف نمی‌شود؛ همان مدل‌ها به پنل جدید وصل می‌شوند.
- کاربر سایت نباید برای مدیریت محتوا وارد `/admin/` شود.
- مسیر پیشنهادی پنل جدید: `/dashboard/` یا `/site-admin/`.
- همه صفحات داشبورد باید permission-based باشند.
- فرم‌ها باید validation، پیام موفق/خطا، جستجو، فیلتر، صفحه‌بندی و عملیات امن داشته باشند.
- فایل‌ها و تصاویر از media/static کنترل‌شده مدیریت شوند و روی سرور production بدون خطای 404 کار کنند.

## Stage 27 — پایه داشبورد اختصاصی مدیریت

هدف: ساخت اسکلت پنل جدید بدون حذف Django Admin.

وظایف:
- ایجاد اپ جدید مثل `dashboard` یا `sitepanel`.
- ساخت layout اختصاصی پنل با RTL، فونت local، تم تیره/طلایی و طراحی هماهنگ با سایت.
- مسیرهای اصلی:
  - `/dashboard/login/`
  - `/dashboard/logout/`
  - `/dashboard/`
- داشبورد اولیه با کارت‌های خلاصه:
  - تعداد لیدهای جدید
  - پیام‌های تماس جدید
  - تعداد مقاله‌ها
  - وضعیت صفحات CMS
  - وضعیت SEO
- محافظت مسیرها با login و permission.
- اضافه کردن sidebar اختصاصی با منوهای مرحله‌ای.
- ثبت در CHANGELOG و roadmap.

## Stage 28 — مدیریت صفحه اول داخل پنل جدید

هدف: جایگزینی مدیریت Home CMS از Django Admin با صفحات اختصاصی.

صفحات پیشنهادی:
- `/dashboard/home/hero/` مدیریت Hero صفحه اول
- `/dashboard/home/quick-proofs/` مدیریت مزیت‌های سریع
- `/dashboard/home/stats/` مدیریت آمارها
- `/dashboard/home/services/` مدیریت خدمات اصلی
- `/dashboard/home/modules/` مدیریت ماژول‌ها
- `/dashboard/home/workflow/` مدیریت مراحل همکاری

نیازمندی‌ها:
- افزودن/ویرایش/فعال/غیرفعال کردن آیتم‌ها
- مرتب‌سازی آیتم‌ها با فیلد order
- preview سریع متن و تصویر
- حفظ fallback قبلی در صورت نبود داده

## Stage 29 — مدیریت صفحات داخلی

هدف: مدیریت کامل صفحات داخلی از پنل جدید.

صفحات:
- درباره ما
- تماس با ما
- مطالعه موردی
- امکانات
- قیمت‌ها
- پلن‌ها
- سوالات متداول

امکانات:
- ویرایش عنوان، زیرعنوان، توضیح، hero image و CTA
- مدیریت آیتم‌های هر سکشن
- فعال/غیرفعال کردن سکشن‌ها
- جستجو و فیلتر بر اساس نوع صفحه و نوع سکشن
- مدیریت تصاویر اعضای تیم و کارت‌های درباره ما

## Stage 30 — مدیریت لیدها و پیام‌های تماس

هدف: تبدیل فرم‌ها به CRM سبک داخل پنل اختصاصی.

امکانات:
- لیست لیدها با فیلتر وضعیت، اولویت، منبع، تاریخ و مسئول
- صفحه جزئیات لید
- تغییر وضعیت لید
- ثبت یادداشت داخلی
- تعیین مسئول پیگیری
- تعیین تاریخ پیگیری بعدی
- خروجی Excel/CSV
- لیست پیام‌های تماس و تغییر وضعیت پیام
- نمایش منبع ثبت، UTM، referrer، IP و user-agent در صفحه جزئیات

## Stage 31 — مدیریت بلاگ و محتوا

هدف: مدیریت محتوای مارکتینگ بدون Django Admin.

امکانات:
- لیست مقاله‌ها
- ایجاد/ویرایش مقاله
- مدیریت slug، تصویر شاخص، خلاصه، متن، وضعیت انتشار
- دسته‌بندی و برچسب‌ها در صورت وجود یا اضافه‌سازی تدریجی
- نمایش preview مقاله
- مدیریت SEO مقاله

## Stage 32 — مدیریت FAQ و قیمت‌ها/پلن‌ها

هدف: صفحات پرتکرار فروش از پنل جدید قابل مدیریت شوند.

امکانات FAQ:
- افزودن/ویرایش سوال و جواب
- دسته‌بندی FAQ
- فعال/غیرفعال کردن سوالات
- ترتیب نمایش

امکانات قیمت/پلن:
- مدیریت پلن‌ها
- ویژگی‌های هر پلن
- قیمت‌ها با واحد «میلیون تومان»
- badge پلن پیشنهادی
- فعال/غیرفعال کردن پلن

## Stage 33 — مدیریت SEO و تنظیمات سایت

هدف: جایگزینی کامل مدیریت SEO از Django Admin با UI اختصاصی.

امکانات:
- تنظیمات عمومی SEO سایت
- meta title/description/keywords هر صفحه
- canonical و robots
- Open Graph image
- Schema type
- preview کارت اشتراک‌گذاری
- مدیریت `robots.txt` و `llms.txt` از پنل در صورت نیاز

## Stage 34 — مدیریت رسانه‌ها و فایل‌ها

هدف: آپلود و انتخاب تصاویر سایت از پنل جدید.

امکانات:
- آپلود تصویر
- لیست رسانه‌ها
- انتخاب تصویر برای Hero، اعضای تیم، مقاله‌ها و OG image
- حذف امن فایل‌های استفاده‌نشده
- نمایش مسیر فایل و preview
- validation برای نوع و حجم فایل

## Stage 35 — امنیت، نقش‌ها و دسترسی‌ها

هدف: آماده‌سازی پنل برای استفاده واقعی تیم.

امکانات:
- نقش‌ها: مدیر کل، محتوا، فروش، پشتیبانی، SEO
- سطح دسترسی صفحه‌ای
- audit log برای تغییرات مهم
- ثبت آخرین ورود و IP
- محدودسازی عملیات حذف
- پیام تایید قبل از عملیات حساس

## Stage 36 — تجربه کاربری و Production polish پنل

هدف: تبدیل داشبورد به ابزار روزمره قابل استفاده.

امکانات:
- جستجوی سراسری پنل
- breadcrumb
- empty state حرفه‌ای
- toast و modalهای تایید
- loading state
- pagination استاندارد
- mobile responsive کامل
- تست collectstatic، static/media، WhiteNoise و deployment

## اولویت اجرای بعدی

قدم بعدی قطعی: **Stage 27 — ساخت پایه داشبورد اختصاصی مدیریت**.

در Stage 27 هنوز لازم نیست همه مدل‌ها صفحه اختصاصی داشته باشند؛ هدف ساخت فریم اصلی پنل، لاگین، layout، sidebar، dashboard summary و آماده‌سازی مسیر برای انتقال قابلیت‌های Stage 23 تا Stage 26 به پنل جدید است.

## وضعیت اجرای Stage 27

Stage 27 اجرا شد و اسکلت پنل اختصاصی آماده است:

- مسیرهای عملیاتی: `/dashboard/login/`، `/dashboard/logout/` و `/dashboard/`
- محافظت دسترسی با کاربر staff/superuser
- layout مستقل RTL با سایدبار، topbar، کارت‌های KPI و جدول‌های خلاصه
- صفحات نقشه راه برای بخش‌های بعدی
- مرحله بعدی قطعی: **Stage 28 — مدیریت واقعی صفحه اول داخل پنل جدید**

از Stage 28 باید فرم‌های واقعی مدل‌های `HomeHeroContent` و `HomeContentItem` داخل همین پنل ساخته شود و کاربر دیگر برای مدیریت صفحه اول وارد Django Admin نشود.

## وضعیت پس از Stage 28

Stage 28 اجرا شد و بخش **مدیریت صفحه اول** از حالت placeholder خارج شد.

قابلیت‌های آماده‌شده:
- ویرایش Hero صفحه اول از `/dashboard/home/`
- افزودن آیتم جدید برای سکشن‌های صفحه خانه
- ویرایش آیتم‌های موجود
- فعال/غیرفعال کردن آیتم‌ها
- حذف آیتم‌ها
- گروه‌بندی آیتم‌ها بر اساس سکشن
- پیش‌نمایش سریع صفحه اول سایت

نتیجه:
- مدل‌های `HomeHeroContent` و `HomeContentItem` حالا UI اختصاصی دارند.
- برای مدیریت صفحه اول، نیاز به استفاده از Django Admin نیست.
- مرحله بعدی قطعی: **Stage 29 — مدیریت صفحات داخلی داخل داشبورد اختصاصی** برای مدل‌های `PageContent` و `PageContentItem`.

## وضعیت پس از Stage 29

Stage 29 اجرا شد و بخش **مدیریت صفحات داخلی** از حالت placeholder خارج شد.

قابلیت‌های آماده‌شده:
- انتخاب صفحه داخلی از تب‌های `/dashboard/pages/`
- ویرایش تنظیمات اصلی صفحه شامل تیتر، توضیح، تصویر Hero و وضعیت فعال
- ویرایش SEO هر صفحه شامل meta title/description/keywords، canonical، robots، OG و Schema
- افزودن آیتم جدید برای سکشن‌های هر صفحه
- ویرایش، فعال/غیرفعال‌سازی و حذف آیتم‌های `PageContentItem`
- راهنمای کد سکشن‌های قابل استفاده برای هر صفحه
- پیش‌نمایش سریع صفحه واقعی سایت

نتیجه:
- مدل‌های `PageContent` و `PageContentItem` حالا UI اختصاصی دارند.
- برای مدیریت صفحات داخلی، نیاز به استفاده از Django Admin نیست.
- مرحله بعدی قطعی: **Stage 30 — مدیریت لیدها و پیام‌ها داخل داشبورد اختصاصی** برای مدل‌های `LeadRequest` و `ContactMessage`.

## Stage 30 - مدیریت لیدها و پیام‌ها در داشبورد اختصاصی
- مسیر `/dashboard/leads/` از حالت placeholder خارج شد و به CRM سبک اختصاصی تبدیل شد.
- لیست لیدها و پیام‌های تماس با tab جداگانه، جستجو و فیلتر وضعیت/اولویت/صفحه مبدا اضافه شد.
- تغییر وضعیت و اولویت گروهی برای لیدها و پیام‌ها اضافه شد.
- صفحه جزئیات لید با مدیریت وضعیت، اولویت، مسئول پیگیری، زمان پیگیری بعدی، آخرین تماس و یادداشت داخلی اضافه شد.
- صفحه جزئیات پیام تماس با مدیریت وضعیت، اولویت و یادداشت داخلی اضافه شد.
- خروجی Excel/CSV برای لیدها و پیام‌ها از داخل پنل اختصاصی اضافه شد.
- این مرحله migration جدید ندارد و از مدل‌های Stage 25 استفاده می‌کند.


## وضعیت Stage 31
- انجام شد: مدیریت بلاگ و FAQ داخل داشبورد اختصاصی.
- مسیرهای اضافه‌شده:
  - `/dashboard/content/`
  - `/dashboard/content/posts/new/`
  - `/dashboard/content/posts/<id>/`
  - `/dashboard/content/faqs/new/`
  - `/dashboard/content/faqs/<id>/`
- قابلیت‌ها: جستجو، فیلتر، ایجاد، ویرایش، حذف امن، تغییر وضعیت گروهی، مدیریت انتشار مقاله، مدیریت مطلب ویژه، مدیریت SEO مقاله و مدیریت ترتیب FAQ.

## مرحله بعدی پیشنهادی - Stage 32
- مدیریت قیمت‌ها، پلن‌ها و FAQهای تجاری داخل داشبورد اختصاصی.
- تکمیل UI برای مدیریت ساختار پلن‌ها، highlightها، مقایسه امکانات و CTAهای قیمت‌گذاری.

## وضعیت Stage 32
- صفحه اختصاصی «قیمت‌ها و پلن‌ها» در داشبورد جدید پیاده‌سازی شد.
- مدیریت صفحه قیمت‌ها و صفحه پلن‌ها، آیتم‌های سکشن‌ها، فعال/غیرفعال‌سازی، حذف، ترتیب نمایش و پیش‌نمایش انجام شد.
- مرحله بعدی: Stage 33 برای SEO و تنظیمات سایت داخل همین داشبورد اختصاصی.

## Stage 32.1 - ویدیوی اصلی صفحه اول
- تصویر Hero صفحه اصلی با ساختار ویدیوی HTML5 جایگزین شد.
- مسیر آماده ویدیو: `landing/static/landing/videos/home-hero.mp4`.
- poster ویدیو فعلاً از تصویر Hero فعلی خوانده می‌شود تا تا زمان جایگذاری فایل ویدیو ظاهر صفحه خراب نشود.
- استایل ریسپانسیو ویدیو با تم تیره/طلایی و حالت شبیه کارت ویدیویی اضافه شد.
- فایل راهنما `HOMEPAGE_VIDEO_ASSETS_FA.md` برای جایگذاری ویدیو اضافه شد.

## Stage 32.2 - Homepage video story sections
- Added four structured video story sections to the homepage after the hero/stats area.
- Prepared video asset paths: `home-section-1.mp4` to `home-section-4.mp4` under `landing/static/landing/videos/`.
- Added responsive RTL video/text layout matching the requested reference: video beside long copy and benefit items.
- Preserved Stage 32.1 hero video and all custom dashboard work through Stage 32.

## Stage 32.3 - About page video section
- Added a new video section to the About page with a responsive HTML5 video frame.
- Prepared `landing/static/landing/videos/about-story.mp4` as the expected video path.
- Used the existing `about_workspace.png` image as the default poster so the page stays stable before the video file is uploaded.
- Added copy and three benefit points explaining the story/team/product vision around the video.
- Synced CSS and video-folder README files with `staticfiles`.

## به‌روزرسانی بعد از Stage 32.4
قبل از اتصال مستقیم به نسخه‌های دمو، مدل `DemoRequest` و فرم عمومی درخواست دمو ساخته شد. مرحله بعد باید مدیریت همین درخواست‌ها در داشبورد اختصاصی و سپس تولید لینک امن ورود به نسخه‌های دمو بهنیکو/سیتباک باشد.

## به‌روزرسانی Stage 32.5
قبل از ادامه Stage 33، مسیر دموها تکمیل شد: ثبت درخواست دمو حالا لینک امن موقت می‌سازد و کاربر را به صفحه انتخاب نسخه دمو هدایت می‌کند. مرحله بعدی، داشبورد مدیریت درخواست‌های دمو و ارسال لینک/یادداشت داخلی است.


## Stage 32.6 - تکمیل مدیریت درخواست‌های دمو
- مسیر `/dashboard/demos/` به پنل اختصاصی اضافه شد.
- درخواست‌های دمو، لینک امن، وضعیت ارسال لینک، تعداد ورود و یادداشت داخلی از پنل جدید مدیریت می‌شوند.
- این مرحله جایگزین وابستگی به Django Admin برای DemoRequest است.
- مرحله بعد: ربات بله و اتصال گفتگوها/درخواست‌ها به داشبورد.


## Stage 32.7 - زیرساخت ربات بله
- مدل‌های گفتگو، پیام و تنظیمات ربات اضافه شدند.
- ربات می‌تواند درخواست مشاوره را به LeadRequest و درخواست دمو را به DemoRequest تبدیل کند.
- Polling و Webhook آماده شد.
- مرحله بعد: ساخت صفحه کامل مدیریت گفتگوهای بله داخل داشبورد اختصاصی.

## Stage 32.8 - مدیریت ربات بله در داشبورد اختصاصی
- صفحه `/dashboard/bale/` برای مدیریت گفتگوهای ربات بله ساخته شد.
- تنظیمات ربات، فیلتر گفتگوها، خروجی، صفحه جزئیات گفتگو، ریست جریان مکالمه و ارسال پاسخ اپراتور از داخل داشبورد اضافه شد.
- پیام‌های مرتبط با LeadRequest و DemoRequest در صفحه جزئیات قابل مشاهده و پیگیری هستند.

## Stage 32.14 - اصلاح نگاشت ویدیوهای صفحه اصلی
- جابه‌جایی ویدیو و کاور دو بخش «یکپارچگی» و «شخصی‌سازی» انجام شد.
- تغییر محدود به نگاشت assetهای صفحه اصلی بود و ساختار داشبورد، فرم دمو، ربات بله و سایر صفحات بدون تغییر حفظ شدند.

## Stage 33 - مدیریت SEO و تنظیمات سایت
- مسیر `/dashboard/seo/` به صفحه واقعی مدیریت SEO و تنظیمات سایت تبدیل شد.
- تنظیمات SiteSettings، SEO صفحات داخلی، SEO سریع مقاله‌ها، پیش‌نمایش Open Graph، robots و llms داخل داشبورد اختصاصی مدیریت می‌شوند.
- این مرحله migration جدید ندارد و از فیلدهای موجود استفاده می‌کند.

## Stage 34 - اصلاح صفحه درباره ما
- چیدمان بخش اول صفحه درباره ما اصلاح شد: تصویر ساختمان در دسکتاپ سمت چپ و متن سمت راست قرار گرفت.
- تصویر ساختمان به asset تمیزتر تبدیل شد و پنل آمار زیر تصویر به‌صورت HTML/CSS بازسازی شد تا اضافات بریده‌شده داخل عکس نمایش داده نشود.
- تصاویر واقعی آقایان شیروانی، فریدی و فرامرزیان در بخش هیئت مدیره جایگزین شدند و مسیرهای CMS/seed/fallback هم به‌روزرسانی شد.
- این مرحله migration جدید ندارد.

## Stage 35 - دیوار محصولات سیت‌باک در صفحه اصلی
- بخش «سیتباک در یک نگاه» حذف شد.
- دیوار محصولات سیت‌باک با HTML/CSS واقعی و بدون استفاده از تصویر ثابت بعد از ویدیوی اول اضافه شد.
- هشت کارت محصولات با رنگ‌بندی نئونی و ریسپانسیو پیاده‌سازی شدند.

## Stage 36 - پشت کارت تعاملی محصولات
- بخش «نمای محصول» حذف شد.
- کارت‌های محصولات به flip-card تبدیل شدند و پشت هر کارت جزئیات محصول نمایش داده شد.

## Stage 37 - فشرده‌سازی جزئیات پشت کارت‌ها
- اندازه متن و فاصله آیتم‌های پشت کارت محصولات کاهش یافت تا همه موارد در پشت کارت کامل دیده شوند.
- این مرحله فقط CSS صفحه اصلی را اصلاح می‌کند و migration جدید ندارد.

## Stage 38 - اصلاح نام شرکت در صفحه درباره ما
- نام شرکت در صفحه درباره ما به «پیشرو برنامه آمارد» تغییر کرد.
- متن کارت معرفی و بخش پشتوانه اجرایی با نام جدید هماهنگ شد.
- seed اولیه صفحه درباره ما نیز برای نصب‌های تازه اصلاح شد.
- این مرحله migration جدید ندارد.

## Stage 39 - پاکسازی متن‌های آماده‌سازی از صفحات عمومی
- متن راهنمای «فایل ویدیو را با نام home-hero.mp4...» از Hero صفحه اصلی حذف شد.
- متن آماده‌سازی ویدیوهای پایین صفحه اصلی با متن نهایی و بازاریابی جایگزین شد.
- نمایش مسیر فایل‌های ویدیویی زیر ویدیوهای صفحه اصلی و صفحه درباره ما حذف شد.
- چند عبارت نمایان که حس تستی/ناتمام بودن سایت می‌دادند در صفحات عمومی، صفحه تماس و متن‌های ویدیویی اصلاح شدند.
- راهنمای داخلی فایل‌های ویدیویی بازنویسی شد تا فقط در مستندات پروژه باقی بماند و در UI نمایش داده نشود.

## Stage 40 - اصلاح موبایل صفحه اصلی
- منوی موبایل صفحه عمومی سایت دوباره پایدار شد و روی موبایل به صورت overlay باز می‌شود.
- ویدیوی اول صفحه اصلی فقط در موبایل به بالاترین بخش محتوایی منتقل شد تا قبل از کارت‌ها و آیتم‌ها دیده شود.
- ریسپانسیو دسکتاپ بدون تغییر حفظ شد.
## Stage 41 - اصلاح لایه منوی موبایل
- منوی موبایل با z-index امن‌تر و header sticky در موبایل بالاتر از ویدیوی Hero قرار گرفت.
- overlay پس‌زمینه برای خوانایی منو هنگام باز بودن اضافه شد.
- دسکتاپ بدون تغییر حفظ شد.


## Stage 42 - اصلاح نمایش منوی موبایل سایت عمومی
- منوی موبایل سایت عمومی از وابستگی به stacking context هدر/ویدیو جدا شد و به‌صورت body-level portal نمایش داده می‌شود.
- مشکل مات شدن کل صفحه بدون دیده شدن آیتم‌های منو در موبایل رفع شد.
- این تغییر فقط سمت فرانت عمومی است و روی داشبورد اختصاصی، دسکتاپ و محتوای صفحات اثر functional ندارد.

## Stage 43 - هماهنگی محتوای عمومی صفحه اصلی
- حذف آمار اعتماد +۳۳۰ و حذف کارت «قدم بعدی» از صفحه اصلی انجام شد.
- خوانایی بخش «مسیر همکاری» در فرانت عمومی افزایش یافت.
- داشبورد اختصاصی و مدل‌ها بدون تغییر functional حفظ شدند.


## Stage 44 - اصلاح UI صفحه اصلی
- تغییر جزئی عنوان کارت آژیر از «مدیریت وظایف» به `BAU` در بخش محصولات صفحه اصلی انجام شد.
- این تغییر مدل، migration یا منطق داشبورد را تغییر نمی‌دهد.


## Stage 45 - بازطراحی کارت TMO / بینا در دیوار محصولات
- ادامه از Stage 44 بدون rollback انجام شد.
- کارت زرد «پکیج های سیت باک / سیت باک» در بخش محصولات صفحه اصلی به کارت جدید `TMO / بینا` تبدیل شد.
- در جلوی کارت، عنوان لاتین `TMO`، نام فارسی `بینا` و یک نشان جدید مبتنی بر آیکون چشم با استایل هماهنگ با سایر کارت‌ها پیاده‌سازی شد.
- پشت کارت به ساختار استاندارد Flip Card برگشت و متن‌ها با این موارد جایگزین شدند: «کنترل فرآیند های واسپاری شده»، «چکاپ تسک ها»، «بررسی علل عدم اجرا یا تاخیر».
- رنگ‌بندی، ریسپانسیو، انیمیشن Hover و سایر کارت‌های بخش محصولات بدون تغییر باقی ماندند.
- migration جدیدی نیاز نبود.


## Stage 46 - وسط‌چین شدن کارت‌های صفحه امکانات
- ادامه از Stage 45 بدون rollback انجام شد.
- در صفحه امکانات، گرید کارت‌های «دسترسی نقش‌محور / اتصال‌پذیری» و «کاربردهای واقعی سیتباک» روی دسکتاپ وسط‌چین شدند.
- برای این دو سکشن، عرض گرید محدود و چینش دو ستونه‌ی متعادل اعمال شد تا کارت‌ها به‌جای چسبیدن به راست، در مرکز صفحه قرار بگیرند.
- ریسپانسیو موبایل و تبلت حفظ شد و در عرض‌های پایین‌تر، کارت‌ها همچنان تک‌ستونه نمایش داده می‌شوند.
- migration جدیدی نیاز نبود.


## Stage 47 - وسط‌چین شدن کارت‌های صفحه درباره ما
- ادامه از Stage 46 بدون rollback انجام شد.
- کارت‌های صفحه درباره ما در سکشن‌های مسیر رشد، اعضای هیئت مدیره، رسالت/ارزش‌ها و نحوه همکاری وسط‌چین شدند.
- برای گریدهای کارت صفحه درباره ما، max-width و justify-content مناسب اضافه شد تا کارت‌ها در دسکتاپ در مرکز صفحه قرار بگیرند.
- ریسپانسیو موبایل و تبلت حفظ شد و در عرض‌های پایین‌تر کارت‌ها به‌صورت تک‌ستونه یا دو‌ستونه نمایش داده می‌شوند.
- سایر صفحات، منوی موبایل و تغییرات مراحل قبلی بدون تغییر باقی ماندند.
- migration جدیدی نیاز نبود.


## Stage 48 - حذف بخش هسته‌های اصلی محصول از صفحه اصلی
- ادامه از Stage 47 بدون rollback انجام شد.
- سکشن «هسته‌های اصلی محصول» شامل عنوان «ماژول‌های مهم، بدون شلوغی اضافه» و کارت‌های ماژول‌های اصلی از صفحه اصلی حذف شد.
- صفحه امکانات و سایر بخش‌های سایت بدون تغییر باقی ماندند.
- بخش «مسیر همکاری» بلافاصله بعد از بخش ویدیوهای معرفی نمایش داده می‌شود.
- migration جدیدی نیاز نبود.


## Stage 49 - اصلاح مقدار رضایت کاربران در صفحه اصلی
- ادامه از Stage 48 بدون rollback انجام شد.
- مقدار باکس «رضایت کاربران» در آمارهای صفحه اصلی از ۹۸٪ به ۱۰۰٪ تغییر کرد.
- seed/fallback محتوای صفحه اصلی نیز برای نصب‌های تازه هماهنگ شد.
- ساختار، چیدمان، موبایل، دسکتاپ و سایر بخش‌ها بدون تغییر باقی ماندند.
- migration جدیدی نیاز نبود.


## Stage 50 - اصلاح قطعی مقدار رضایت کاربران از CMS
- ادامه از Stage 49 بدون rollback انجام شد.
- مشکل باقی‌ماندن مقدار ۹۸٪ در صفحه اصلی زمانی که مقدار قدیمی از دیتابیس CMS خوانده می‌شد، برطرف شد.
- بعد از اعمال overrideهای CMS، مقدار آیتم «رضایت کاربران» در context صفحه اصلی به‌صورت قطعی روی ۱۰۰٪ تنظیم می‌شود.
- migration جدید `0013_update_home_satisfaction_stat` اضافه شد تا دیتابیس‌های موجود هم مقدار ذخیره‌شده آیتم «رضایت کاربران» را به ۱۰۰٪ به‌روزرسانی کنند.
- سایر آمارها، چیدمان دسکتاپ/موبایل و تغییرات قبلی حفظ شدند.
